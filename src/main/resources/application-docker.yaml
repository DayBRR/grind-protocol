spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/grind_protocol
    username: grind_protocol
    password: grind_protocol
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    issuer: grind-protocol
    expiration: 900000
    secret-key: aTSvmUXBiooktgiD5cdyw3IM0W+8AMfhh5nRiCZmQ9o=